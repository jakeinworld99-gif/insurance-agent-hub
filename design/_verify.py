# Ad-hoc verification script for KAN-42 mockup.html (kept for reference)
# This Muse profile has no shell tool, so this was never executed as a runner.
# The actual checks ran via read_file + search_files on the mockup directly.
# Do not ship this file with the design; delete before any Vercel deploy.
